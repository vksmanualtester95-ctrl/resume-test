# macOS
.DS_Store
.AppleDouble
.LSOverride

# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Claude session/config (local-only — generated per session)
.claude/

# Personal artifacts — NEVER push these (contain phone + email)
Profile Linkedin.pdf
input/
output/

# Local-only contact override (phone, email) — never commit
profile/master.local.yaml
# But the schema example IS committed:
# profile/master.local.yaml.example


# Editor / OS
.vscode/
.idea/
*.swp
*.swo
*~
Thumbs.db
